# CellProfiler Backend Optimization Tracker

This file tracks CellProfiler-compatible modules that still contain slow
Python, skimage/scipy, or centrosome-heavy execution paths. Entries are removed
only after a typed OpenHCS backend is implemented, selected explicitly or by a
proven default, and covered by parity/performance evidence.

## Active Hot Paths

| Module / backend | Current issue | Target |
| --- | --- | --- |
| `MeasureObjectSizeShape` / shape geometry | Area/bbox/centroid/equivalent diameter/extent/moments/inertia/perimeter/euler now use shared `openhcs.processing.backends.analysis.region_properties` Numba default. Convex area/solidity use a direct exact convex-hull helper instead of `regionprops_table` wrapper overhead. Orientation is now computed in the shared Numba backend with skimage-0.18-compatible two-stage crop-moment reduction order instead of a current-skimage leaf. Direct Yeast label oracle against native CP showed the aggregate orientation mean exactly matches native (`21.537426810670524`) with only 2/2450 residual per-object diagonal tie signs, which runtime equivalence treats as stable orientation sign ambiguity. Explicit centrosome leaves remain under `LEGACY_FAST`/`CENTROSOME` for Feret/min enclosing circle/Zernike center-radius. | Rerun focused Yeast parity with the shared Numba orientation path, then move Feret/min enclosing circle/Zernike center-radius into pure typed providers. |
| `CorrectIlluminationCalculate` median smoothing | Fixed: median-filter illumination smoothing now uses typed `RankMedianSmoothingBackendStrategy` with Numba sliding-histogram default as of 2026-05-03 18:55 EDT and an explicit `NATIVE` skimage reference provider. Bit-equivalent to `skimage.filters.rank.median` on masked/bordered test data and to the prior exact Numba implementation on a 160x180 radius85 crop; that crop improved from 57.78s to 0.023s (~2550x). `ExampleCometAssay` stayed green against a fresh native reference. | Keep under parity regression and reuse the sliding histogram for other rank-filter-compatible CellProfiler paths. |
| `IdentifyPrimaryObjects` threshold diagnostics | Fixed: threshold estimation now follows CellProfiler library semantics by estimating global/adaptive thresholds from the raw image and applying `threshold_smoothing_scale` only when producing the binary mask. IPO foreground/background diagnostics now use the threshold binary rather than post-filtered labels. `ExampleCometAssay` moved from 290 diffs to green against a fresh native reference as of 2026-05-03 18:55 EDT. | Keep under threshold regression tests and avoid method-specific diagnostic shortcuts. |
| `IdentifyPrimaryObjects` threshold application smoothing | Fixed: threshold binary-mask smoothing now mirrors native CellProfiler's `centrosome.smooth.smooth_with_function_and_mask` path by applying `scipy.ndimage.gaussian_filter(..., mode="constant", cval=0)` to the image and mask normalization terms. This removed the `ExampleHuman` 13-pixel `Nuclei` binary divergence and label/measurement cascade; focused parity at `/tmp/openhcs_cppipe_parity_focus_human_threshold_constant_20260503_211934` reports `differences=0` with uncached OpenHCS runtime `7.142s`. | Keep smoothing-boundary semantics shared with the explicit `Smooth` backend and avoid independent boundary defaults in threshold code. |
| `CorrectIlluminationCalculate` background block partitioning | Fixed: block labels now use CellProfiler-compatible floor block counts and ceil-scaled boundaries through the typed morphology backend as of 2026-05-03 16:28 EDT. `ExampleYeastPatches` moved from 66 semantic diffs to green. Numba block-label microbench is sub-millisecond after compile on the Yeast image shape. | Keep under Convex Hull/Background regression coverage and avoid reintroducing independent block heuristics. |
| `CorrectIlluminationCalculate` automatic smoothing size | Fixed: automatic illumination smoothing size now mirrors native CellProfiler 3.9 source as `min(30, max(image_shape) / 40.0)` instead of deriving from `object_width`. This moved `ExampleYeastColonies` to green at `/tmp/openhcs_cppipe_parity_focus_yeast_colonies_auto_smoothing_20260503_213655` with `differences=0`; the prior `object_width * 2.0` value made Yeast use filter size `20` instead of CP's `27.9`, shifting the corrected red image and masked log-Multi-Otsu threshold. | Keep the filter-size strategy source-derived and shared across all illumination methods. |
| `Align` | Mutual-information search now lives in typed `AlignmentBackendStrategy` with Numba default as of 2026-05-03 14:58 EDT. Histogram buffers are reused and entropy is computed over touched bins, but Yeast still spends `1.679s` in `Align`, so full candidate rescans/cold JIT remain material. | Profile actual stacked Align inputs and replace full rescans with an equivalent incremental or tiled MI strategy only if native offset/parity remains exact. |
| `IdentifyPrimaryObjects` | Steady-state YeastPatches profile: declumping smoothing is ~0.007s after Numba compile vs ~0.037s scipy, watershed ~0.11s, maxima ~0.17s. Fixed automatic intensity declump suppression geometry as of 2026-05-03 16:38 EDT; `ExampleVitra` improved from 154 diffs to 23 with object counts matching, and `ExamplePercentPositive` stayed green. Replaced the prior fragment reconciliation with Numba pre-declump basin restoration as of 18:02 EDT: removed watershed basins are restored only when their original connected component has exactly one surviving object identity. `ExampleTrackObjects` and `ExampleFlyURL` are both green with this rule. As of 21:05 EDT, manual declumping footprint/smoothing now mirrors native CP source exactly: `strel_disk(max(1, maxima_suppression_size - 0.5))` and `sigma = filter_size / 2.35` for all declump methods. This made `ExampleSpeckles` green, and module-8 `raw_maxima`/`shrunk_maxima`/separated labels matched native CP byte-for-byte. The no-resize declumping seed path has a Numba backend override. Full fresh process still pays Numba cold compile/discovery. | Move distance primitives and any remaining low-res resize path into typed backends, then reduce cold-start cost where possible. |
| `IdentifySecondaryObjects` | Fixed Distance-B seed-label contract as of 2026-05-03 16:52 EDT: unedited primary labels are remapped to final accepted object IDs, and rejected edge constraints receive temporary non-conflicting IDs. The remap is Numba-accelerated; `ExampleVitra` moved from 23 diffs to green. Distance transforms use a typed Numba default; propagation remains explicit centrosome exact provider. | Add a Numba propagation provider only after exact priority/cost semantics are proven. |
| `ClassifyObjects` / runtime measurement lookup | Fixed: repeated `labels == label` remap was removed because native CP `ClassifyObjects` records measurements rather than producing a classified label image. Runtime measurement binding now uses generic direct column lookup for both columnar and homogeneous wide row tables and dense label-id discovery via `bincount`. Generic row-field and measurement-token caching removed repeated full-table scans during runtime special-input binding. Yeast runtime-only evidence at `/tmp/openhcs_cppipe_runtime_yeast_measurement_lookup_fast_20260503_231149` completed in `9.819s`; `ClassifyObjects` dropped to `0.340s` total. | Profile remaining wrapper/materialization overhead only if `ClassifyObjects` reappears as a top hotspot; current Yeast hot steps are `MeasureObjectSizeShape`, `Align`, and `IdentifyPrimaryObjects`. |
| `RelateObjects` | Relationship kernels now live in typed `ObjectRelationshipBackendStrategy` with Numba default as of 2026-05-03 14:58 EDT. | Prove parity/performance on pipelines using parent-child distance measurements. |
| `TrackObjects` | Fixed: centers, overlap matching, and distance matching now live in typed `ObjectTrackingBackendStrategy` with Numba default as of 2026-05-03 18:01 EDT. Synthetic 512x512 microbench: centers improved ~50x (`10.3 ms` to `0.21 ms` per call) and overlap ~1.7x (`0.465 ms` to `0.271 ms`). `ExampleTrackObjects` stayed green. | Extend the backend to LAP/sparse-assignment paths when those settings are enabled. |
| `MeasureTexture` / image quality | Fixed for MeasureTexture object mode: object crop and Haralick primitives now use typed Numba defaults with explicit native/reference providers as of 2026-05-03 18:02 EDT. `ExampleFlyURL` MeasureTexture completed in under 1s for three site invocations (`18:02:03.921` to `18:02:04.777`) and stayed green. | Move remaining image-quality texture/centrosome paths to typed providers. |
| `Smooth` Gaussian filter | Fixed: Gaussian smoothing now uses CellProfiler's constant-zero boundary semantics (`scipy.ndimage.gaussian_filter(..., mode="constant", cval=0)`) in both explicit Numba and native providers as of 2026-05-03 20:50 EDT. This removed the `ExampleImagingFlowCytometryObjectsInGrid` threshold/object cascade: direct `SmoothedBF`/`EdgedImage`/`MorphBf` deltas against native CP are now float roundoff (`~1.9e-09` max), and the focused parity run at `/tmp/openhcs_cppipe_parity_focus_objects_in_grid_smooth_constant_20260503_204822` reports `differences=0`. | Move the direct smoothing kernels into a typed image-filter backend shared by other convolution-style modules. |
| Object outlines | `IdentifyTertiaryObjects` and `MeasureObjectNeighbors` now route outlines through typed `ObjectOutlineBackendStrategy` with Numba default as of 2026-05-03 14:58 EDT. | Extend same backend to remaining outline-image creation sites where semantics match. |
| Value-only dead image work | Fixed: table-only parity runs now request converter liveness pruning for unmaterialized image artifacts with no downstream consumer, while preserving image artifacts required by skipped `SaveImages`. `ExamplePercentPositive` pruned `OverlayOutlines` and `DisplayDataOnImage`, stayed green, and removed the ~25s `DisplayDataOnImage` hotspot seen in `/tmp/openhcs_cppipe_parity_full_refresh_20260503_185928`. | Keep pruning scoped to explicit value-only runs; do not apply it to normal generated OpenHCS pipelines that need runtime image artifacts. |
| Generic step timing | Fixed: `FunctionStepExecutor` now logs per-step execute/finalize/total elapsed time directly. Current `ExampleYeastPatches` rerun showed `IdentifyObjectsInGrid` at `0.025-0.032s`, `DefineGrid` at `0.210s`, and `MeasureObjectIntensity` at `0.203s`, making later full-run profiling actionable without timestamp reconstruction. | Use these timings during every parity wait to pick measured bottlenecks before adding module-specific optimizations. |

## Backlog

| Module / backend | Current issue | Target |
| --- | --- | --- |
| `EnhanceEdges` | Multiple explicit centrosome filter providers remain. | Replace Sobel/Prewitt/LoG/Roberts/Kirsch/Canny primitives with typed Numba providers where parity permits. |
| `MeasureImageAreaOccupied` | Dense label area/perimeter now routes through shared `LabelRegionPropertiesBackendStrategy`; binary 2-D area/perimeter uses shared Numba Crookes perimeter semantics. 3-D surface area still uses marching cubes. | Keep 2-D perimeter under regression coverage and move 3-D surface-area semantics only after exact parity evidence. |
| `FilterObjects` | Fixed: derived area measurements and size filtering now route through shared `LabelRegionPropertiesBackendStrategy`. | Route any remaining non-area object-stat derivations through shared typed backends as they are proven. |
| `IdentifyObjectsInGrid` | Direct Numba kernels remain embedded in the function module. | Move grid filling, center lookup, circle masking, and guide projection into typed grid backend. |
| `ExpandOrShrinkObjects` / `SplitOrMergeObjects` / `CombineObjects` | Remaining scipy EDT and `labels == label_id` loops. | Reuse Numba EDT/label remap backends. |
| `Zernike` | Mixed Zernike scoring is now named `LEGACY_FAST`; no `NUMBA` Zernike provider is registered while it still depends on centrosome minimum-enclosing-circle semantics. | Finish pure Numba Zernike indexes, center/radius, and polynomial path, then register `CellProfilerBackendProvider.NUMBA`. |
| `MeasureGranularity` | Direct Numba kernels remain embedded in the function module. As of 2026-05-03 20:10 EDT, object means build a compact foreground-pixel index once per object set and reuse it across spectrum iterations; exact microbench on ObjectsInGrid labels showed `0.0` max difference vs the previous full-label scan. Warm per-channel ObjectsInGrid timings are about `0.78-0.96s` at `subsample_size=1.0`, but the fresh-process pipeline still paid cold Numba compile and logged `MeasureGranularity` at `13.605s`. | Move morphology/reconstruction/stat kernels into typed granularity backend, reduce cold compile count/prewarm cost, and then reprofile actual OpenHCS-only runtime without native CellProfiler and semantic snapshot overhead. |
| `EnhanceEdges` | Direct Numba kernels and explicit centrosome filter calls remain in function modules. | Move convolution/filter kernels to typed image-filter backends with explicit centrosome providers only. |
