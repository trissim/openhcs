# arXiv abstract (paper4)

Title: Decision Quotient: A Regime-Sensitive Complexity Theory of Exact Relevance Certification

## Abstract (MathJax, arXiv-ready)

```text
Which coordinates of a decision problem can be hidden without changing the optimal action, and what is the computational cost of determining this? We analyze the complexity of this certification problem across three settings: static (counterexample exclusion), stochastic (conditioning and expectation), and sequential (temporal structure). In the static regime, minimum sufficiency is coNP-complete. In the stochastic regime, preservation and decisiveness separate: preservation is polynomial-time under explicit-state encoding, while decisiveness is PP-hard under succinct encoding with anchor and minimum variants in $\textsf{NP}^{\textsf{PP}}$. In the sequential regime, all queries are PSPACE-complete. We prove an encoding-sensitive contrast between explicit-state tractability and succinct-encoding hardness, identify six natural structural restrictions under which the problem becomes polynomial-time solvable, and derive an integrity-competence trilemma. A Lean 4 artifact mechanically verifies the optimizer-quotient universal property, main reductions, and finite decider core.
```

## Abstract (Unicode, Zenodo-ready)

```text
Which coordinates of a decision problem can be hidden without changing the optimal action, and what is the computational cost of determining this? We analyze the complexity of this certification problem across three settings: static (counterexample exclusion), stochastic (conditioning and expectation), and sequential (temporal structure). In the static regime, minimum sufficiency is coNP-complete. In the stochastic regime, preservation and decisiveness separate: preservation is polynomial-time under explicit-state encoding, while decisiveness is PP-hard under succinct encoding with anchor and minimum variants in NPᴾᴾ. In the sequential regime, all queries are PSPACE-complete. We prove an encoding-sensitive contrast between explicit-state tractability and succinct-encoding hardness, identify six natural structural restrictions under which the problem becomes polynomial-time solvable, and derive an integrity-competence trilemma. A Lean 4 artifact mechanically verifies the optimizer-quotient universal property, main reductions, and finite decider core.
```
