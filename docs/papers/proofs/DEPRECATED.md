This directory has been deprecated and should not be used.

- Rationale: proofs are now organized under each paper subfolder (e.g.
  `docs/papers/paper2_ssot/proofs/`, `docs/papers/paper9_pattern/proofs/`, ...).
- Action taken: `lakefile.lean` removed to avoid accidental builds from this directory.
- Next steps: if you want the whole directory removed or moved, tell me which
  (delete vs move). Default recommendation: move it to
  `docs/papers/_proofs_obsolete/` if you want a reversible operation, or delete
  it permanently if you're confident.

If you want me to fully delete or relocate the directory I can do that now.
