Function registration
=====================

This page moved when its generic implementation was extracted.
:external+metaclass-registry:doc:`metaclass-registry <index>` owns the reusable
registration mechanism. OpenHCS documents only the domain declarations,
adapters, and cross-package invariants that compose it.

See :doc:`nominal_ownership` for the current OpenHCS integration boundary and
:doc:`external_foundations` for the package ownership policy.
