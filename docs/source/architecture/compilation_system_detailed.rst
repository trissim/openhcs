Detailed compiler transition
============================

The former fixed-phase and string-keyed compiler narrative is retained only in
the documentation archive. See :doc:`pipeline_compilation_system` for the
current ``StepSnapshot``, ``CompilationSession``, typed-plan, and
``CompiledExecutionBundle`` architecture.
