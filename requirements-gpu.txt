# GPU dependencies for OpenHCS
# Install with: pip install -r requirements-gpu.txt

# PyTorch
torch
torchvision

# JAX
jaxlib

# CuPy (requires CUDA toolkit)
cupy

# TensorFlow
tensorflow>=2.12.0,<2.16.0  # Use stable TensorFlow instead of nightly
